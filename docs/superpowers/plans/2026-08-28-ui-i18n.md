# 介面多語言（i18n）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把介面上所有使用者看得到的文字抽進 JSON 語言檔，支援繁體中文（台灣）／簡體中文（中國）／英文三種介面語言，並在設定儲存後立即生效。

**Architecture:** 新增 `src/i18n/` 套件（`t(key, **kwargs)` ＋ 三個扁平 JSON 語言檔）與 `src/ui/fonts.py`（字型隨語言切換）。所有呼叫端把「模組層文案常數」下沉成呼叫當下取字串；純邏輯函式（表單驗證、錯誤分類、狀態判定）改為回傳 **key** 而非已翻譯文字，由顯示端翻譯。overlay 常駐視窗改為記住 state key，語言變更時由 `refresh_labels()` 重繪。

**Tech Stack:** Python 3.11+、tkinter／ttk、標準庫 `json`／`locale`／`ctypes`、pytest、uv、PyInstaller。

**Spec:** `docs/superpowers/specs/2026-08-28-ui-i18n-design.md`

## Global Constraints

- **執行環境**：一律用 uv。測試 `uv run pytest`；跑程式 `uv run run.py`。
- **UI 文字語言**：使用者看得到的介面文字維持三語，繁中為 source of truth。
- **log 訊息一律英文**：所有寫進 stdout／stderr 的字串（含 `key=value` 診斷欄位）用英文，前綴對齊既有慣例（`[i18n]`／`[reader]`／`[translate]`／`[settings]`／`[ui]`／`[app]`）。**API 金鑰絕不可寫入 log。**
- **註解／docstring 用繁體中文**，全形標點；模組頂端與公開 function／class 要有 docstring。
- **標點依語言慣例**：繁中全形＋直角引號 `「」`；簡中全形＋彎引號 `“”`；英文半形，破折號 `—` 兩側留空格，錯誤訊息寫成完整句子並加句號，標籤用 sentence case。
- **Commit message 一律英文**，conventional commits（`feat(...)`／`fix(...)`／`refactor(...)`／`test(...)`／`chore(...)`），subject 簡潔。
- **不要 `git push`。** 每個 task 結束時 commit，不推。
- **語言碼固定三個**：`zh-TW`（source）、`zh-CN`、`en`。檔名即語言碼（`src/i18n/zh-TW.json`）。
- **每新增一條文案，三個 JSON 都要同時加**——`tests/test_i18n.py` 的 key 一致性測試會擋下漏翻。
- **YAGNI**：只實作本計畫列出的內容，不預留「未來可能用到」的參數或抽象層。

---

### Task 1: i18n 核心模組

建立語言資源層與打包設定。此時語言檔只放一條 `app.name`，後續任務逐步把文案搬進來。

**Files:**
- Create: `src/i18n/__init__.py`
- Create: `src/i18n/zh-TW.json`
- Create: `src/i18n/zh-CN.json`
- Create: `src/i18n/en.json`
- Modify: `build.spec:4-11`
- Test: `tests/test_i18n.py`

**Interfaces:**
- Consumes: 無（本計畫的第一個任務）
- Produces:
  - `src.i18n.t(key: str, **kwargs) -> str`
  - `src.i18n.set_language(code: str) -> None`
  - `src.i18n.current_language() -> str`
  - `src.i18n.detect_system_language() -> str`
  - `src.i18n.map_locale_name(name: str) -> str`
  - `src.i18n.LANGUAGES: dict[str, str]`（語言碼 → endonym）
  - `src.i18n.SOURCE_LANGUAGE = "zh-TW"`、`src.i18n.DEFAULT_LANGUAGE = "en"`

- [ ] **Step 1: 建立三個語言檔**

`src/i18n/zh-TW.json`：

```json
{
  "app.name": "Wizard101 對話翻譯助手"
}
```

`src/i18n/zh-CN.json`：

```json
{
  "app.name": "Wizard101 对话翻译助手"
}
```

`src/i18n/en.json`：

```json
{
  "app.name": "Wizard101 Chat Translator"
}
```

- [ ] **Step 2: 寫失敗測試**

建立 `tests/test_i18n.py`：

```python
"""i18n 核心：語言檔一致性、fallback 行為、系統語言映射。"""
import json
from pathlib import Path

import pytest

from src import i18n


@pytest.fixture(autouse=True)
def restore_language():
    """還原全域語言，並清掉快取——測試會往 _cache 塞假 key，不清會污染後續測試。"""
    before = i18n.current_language()
    yield
    i18n._cache.clear()
    i18n.set_language(before)


def _load_raw(code: str) -> dict:
    path = Path(i18n.__file__).parent / f"{code}.json"
    return json.loads(path.read_text(encoding="utf-8"))


def test_every_language_has_the_same_keys():
    source = set(_load_raw(i18n.SOURCE_LANGUAGE))
    for code in i18n.LANGUAGES:
        assert set(_load_raw(code)) == source, f"{code} 的 key 與來源語言不一致"


def test_placeholders_match_across_languages():
    import string

    def placeholders(text: str) -> set[str]:
        return {name for _, name, _, _ in string.Formatter().parse(text) if name}

    source = _load_raw(i18n.SOURCE_LANGUAGE)
    for code in i18n.LANGUAGES:
        strings = _load_raw(code)
        for key, template in source.items():
            assert placeholders(strings[key]) == placeholders(template), \
                f"{code} 的 {key} 變數與來源語言不一致"


def test_language_files_have_no_duplicate_keys():
    for code in i18n.LANGUAGES:
        path = Path(i18n.__file__).parent / f"{code}.json"
        # 重複 key 在 json.load 下會被靜默保留後者，用 hook 明確擋下
        with path.open(encoding="utf-8") as handle:
            json.load(handle, object_pairs_hook=i18n._no_duplicates)


def test_t_returns_current_language_string():
    i18n.set_language("en")
    assert i18n.t("app.name") == "Wizard101 Chat Translator"
    i18n.set_language("zh-CN")
    assert i18n.t("app.name") == "Wizard101 对话翻译助手"


def test_t_formats_named_placeholders():
    i18n.set_language("zh-TW")
    i18n._cache["zh-TW"]["test.greet"] = "你好 {name}"
    assert i18n.t("test.greet", name="Amy") == "你好 Amy"


def test_missing_key_falls_back_to_source_language():
    i18n.set_language("en")
    i18n._cache[i18n.SOURCE_LANGUAGE]["test.only_source"] = "只有來源語言有"
    assert i18n.t("test.only_source") == "只有來源語言有"


def test_missing_everywhere_returns_the_key_itself():
    i18n.set_language("en")
    assert i18n.t("test.nowhere") == "test.nowhere"


def test_broken_placeholder_falls_back_to_source_language():
    # 譯者把 {count} 打成 {conut}：該語言的字串無法 format，退回來源語言
    i18n.set_language("en")
    i18n._cache[i18n.SOURCE_LANGUAGE]["test.count"] = "共 {count} 則"
    i18n._cache["en"]["test.count"] = "total {conut}"
    assert i18n.t("test.count", count=3) == "共 3 則"


def test_unknown_language_code_falls_back_to_default():
    i18n.set_language("fr-FR")
    assert i18n.current_language() == i18n.DEFAULT_LANGUAGE


@pytest.mark.parametrize("name, expected", [
    ("zh_TW", "zh-TW"),
    ("zh_HK", "zh-TW"),
    ("zh_MO", "zh-TW"),
    ("zh_CN", "zh-CN"),
    ("zh_SG", "zh-CN"),
    ("en_US", "en"),
    ("ja_JP", "en"),
    ("", "en"),
])
def test_map_locale_name(name, expected):
    assert i18n.map_locale_name(name) == expected


def test_languages_are_listed_as_endonyms():
    assert i18n.LANGUAGES == {
        "zh-TW": "繁體中文（台灣）",
        "zh-CN": "简体中文（中国）",
        "en": "English",
    }
```

- [ ] **Step 3: 執行測試確認失敗**

Run: `uv run pytest tests/test_i18n.py -v`
Expected: FAIL，`ModuleNotFoundError: No module named 'src.i18n'`

- [ ] **Step 4: 實作 `src/i18n/__init__.py`**

```python
"""介面文案資源：依當前介面語言提供字串。

語言檔為扁平 key-value JSON（`<語言碼>.json`），繁體中文（台灣）是來源語言，
也是缺字串時的 fallback 對象——翻譯平台上譯文未完成是常態，介面不可因此炸開。
本模組只負責「給字串」，不碰 UI、不碰 config 讀寫。
"""
import json
import locale
import sys
from pathlib import Path

# 語言碼 → endonym（該語言自稱）。語言選單在任何介面語言下都顯示 endonym、不翻譯。
LANGUAGES: dict[str, str] = {
    "zh-TW": "繁體中文（台灣）",
    "zh-CN": "简体中文（中国）",
    "en": "English",
}

SOURCE_LANGUAGE = "zh-TW"   # 文案來源語言，同時是 fallback 對象
DEFAULT_LANGUAGE = "en"     # 偵測不到或語言碼不認得時的退路

# Windows locale 名稱 → 介面語言碼。未列出者一律退 DEFAULT_LANGUAGE。
_LOCALE_MAP = {
    "zh_TW": "zh-TW", "zh_HK": "zh-TW", "zh_MO": "zh-TW",
    "zh_CN": "zh-CN", "zh_SG": "zh-CN",
}

_current = SOURCE_LANGUAGE
_cache: dict[str, dict[str, str]] = {}


def _i18n_dir() -> Path:
    """語言檔所在目錄。打包（frozen）時語言檔被解壓到 _MEIPASS，與 config 所在的
    exe 旁目錄不同——語言檔是唯讀資源，不能沿用 config.app_dir()。"""
    if getattr(sys, "frozen", False):
        return Path(sys._MEIPASS) / "i18n"
    return Path(__file__).resolve().parent


def _no_duplicates(pairs: list[tuple[str, str]]) -> dict[str, str]:
    """json.load 的 object_pairs_hook：重複 key 預設會被靜默覆蓋，這裡直接擋下。"""
    seen: dict[str, str] = {}
    for key, value in pairs:
        if key in seen:
            raise ValueError(f"duplicate key: {key}")
        seen[key] = value
    return seen


def _load(code: str) -> dict[str, str]:
    if code not in _cache:
        path = _i18n_dir() / f"{code}.json"
        with path.open(encoding="utf-8") as handle:
            _cache[code] = json.load(handle, object_pairs_hook=_no_duplicates)
    return _cache[code]


def current_language() -> str:
    """目前的介面語言碼。"""
    return _current


def set_language(code: str) -> None:
    """切換介面語言；語言碼不認得時退回 DEFAULT_LANGUAGE。"""
    global _current
    if code not in LANGUAGES:
        print(f"[i18n] unknown language: {code}, using {DEFAULT_LANGUAGE}",
              file=sys.stderr)
        code = DEFAULT_LANGUAGE
    _current = code
    _load(code)
    print(f"[i18n] language set: {code}", file=sys.stderr)


def _lookup(key: str) -> str:
    strings = _load(_current)
    if key in strings:
        return strings[key]
    print(f"[i18n] missing key: {key} lang={_current}", file=sys.stderr)
    return _load(SOURCE_LANGUAGE).get(key, key)


def t(key: str, **kwargs) -> str:
    """取當前語言的文案，並以具名變數 format。

    format 失敗（譯者把變數名打錯）時退回來源語言的字串重試——寧可顯示繁中，
    也不要讓整個視窗因為一則譯文而拋例外。"""
    template = _lookup(key)
    try:
        return template.format(**kwargs)
    except (KeyError, IndexError) as exc:
        print(f"[i18n] format failed: key={key} lang={_current} error={exc}",
              file=sys.stderr)
        fallback = _load(SOURCE_LANGUAGE).get(key, key)
        try:
            return fallback.format(**kwargs)
        except (KeyError, IndexError):
            return fallback


def map_locale_name(name: str) -> str:
    """Windows locale 名稱（如 `zh_TW`）→ 介面語言碼；未支援的語言退 DEFAULT_LANGUAGE。"""
    return _LOCALE_MAP.get(name.split(".")[0], DEFAULT_LANGUAGE)


def detect_system_language() -> str:
    """偵測 Windows 使用者介面語言。任何失敗都退 DEFAULT_LANGUAGE 並留下 log。"""
    try:
        import ctypes
        lcid = ctypes.windll.kernel32.GetUserDefaultUILanguage()
        name = locale.windows_locale.get(lcid, "")
    except Exception as exc:
        print(f"[i18n] system language detection failed: {exc}", file=sys.stderr)
        return DEFAULT_LANGUAGE
    code = map_locale_name(name)
    print(f"[i18n] system language detected: lcid={lcid} locale={name} -> {code}",
          file=sys.stderr)
    return code
```

- [ ] **Step 5: 執行測試確認通過**

Run: `uv run pytest tests/test_i18n.py -v`
Expected: PASS（全部）

- [ ] **Step 6: 把語言檔加進打包設定**

`build.spec` 的 `datas` 目前是空列表，改為：

```python
    datas=[("src/i18n/*.json", "i18n")],  # 語言檔：執行期由 src/i18n 依 _MEIPASS 讀取
```

- [ ] **Step 7: 執行全部測試**

Run: `uv run pytest`
Expected: PASS（既有測試不受影響）

- [ ] **Step 8: Commit**

```bash
git add src/i18n tests/test_i18n.py build.spec
git commit -m "feat(i18n): add JSON-backed string catalog with language detection"
```

---

### Task 2: 介面字型模組

字型跟著介面語言走：微軟正黑體是台灣字形標準，簡中使用者看會覺得字形不對；英文介面用 Segoe UI 更自然。同時消除 15 處硬編字族。

**Files:**
- Create: `src/ui/fonts.py`
- Test: `tests/test_fonts.py`

**Interfaces:**
- Consumes: `src.i18n.current_language()`、`src.i18n.set_language()`
- Produces: `src.ui.fonts.ui_font(size: int, weight: str | None = None) -> tuple`

- [ ] **Step 1: 寫失敗測試**

建立 `tests/test_fonts.py`：

```python
"""介面字型：字族隨介面語言切換。"""
import pytest

from src import i18n
from src.ui.fonts import ui_font


@pytest.fixture(autouse=True)
def restore_language():
    before = i18n.current_language()
    yield
    i18n.set_language(before)


def test_font_family_follows_language():
    i18n.set_language("zh-TW")
    assert ui_font(9) == ("Microsoft JhengHei", 9)
    i18n.set_language("zh-CN")
    assert ui_font(9) == ("Microsoft YaHei", 9)
    i18n.set_language("en")
    assert ui_font(9) == ("Segoe UI", 9)


def test_font_accepts_weight():
    i18n.set_language("zh-TW")
    assert ui_font(10, "bold") == ("Microsoft JhengHei", 10, "bold")
```

- [ ] **Step 2: 執行測試確認失敗**

Run: `uv run pytest tests/test_fonts.py -v`
Expected: FAIL，`ModuleNotFoundError: No module named 'src.ui.fonts'`

- [ ] **Step 3: 實作 `src/ui/fonts.py`**

```python
"""介面字型：依當前介面語言選字族。

微軟正黑體雖然顯示得出簡體字，但字形是台灣標準；英文介面用它也不如 Segoe UI。
所有 tkinter 字型都經過這裡，語言一換字型就跟著換。"""
from src.i18n import current_language

_FAMILIES = {
    "zh-TW": "Microsoft JhengHei",
    "zh-CN": "Microsoft YaHei",
    "en": "Segoe UI",
}
_FALLBACK_FAMILY = "Segoe UI"


def ui_font(size: int, weight: str | None = None) -> tuple:
    """回傳 tkinter 字型 tuple（字族取自當前介面語言）。"""
    family = _FAMILIES.get(current_language(), _FALLBACK_FAMILY)
    return (family, size) if weight is None else (family, size, weight)
```

- [ ] **Step 4: 執行測試確認通過**

Run: `uv run pytest tests/test_fonts.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/ui/fonts.py tests/test_fonts.py
git commit -m "feat(ui): add language-aware font helper"
```

---

### Task 3: config 的 `ui_language` 與 `app_name()`

把介面語言接上 config，並讓應用程式名稱可翻譯。

**Files:**
- Modify: `src/config.py:8`（`APP_NAME` → `app_name()`）、`src/config.py:21-41`（新增欄位）
- Modify: `src/main.py:200-213`（啟動時套用語言）
- Modify: `src/reader/overlay.py:345`、`:425`、`:530`（呼叫端）
- Modify: `src/ui/settings.py:7`、`:50`（呼叫端）
- Modify: `src/ui/wizard.py:6`、`:37`（呼叫端）
- Modify: `src/composer/input_box.py:10`、`:46`（呼叫端）
- Test: `tests/test_config.py`

**Interfaces:**
- Consumes: `src.i18n.t()`、`src.i18n.detect_system_language()`、`src.i18n.set_language()`
- Produces:
  - `src.config.app_name() -> str`（取代 `APP_NAME` 常數）
  - `DEFAULT_CONFIG["ui_language"] = None`

- [ ] **Step 1: 寫失敗測試**

在 `tests/test_config.py` 末端加入：

```python
def test_default_config_has_ui_language():
    from src.config import DEFAULT_CONFIG

    assert DEFAULT_CONFIG["ui_language"] is None  # None＝尚未選過，啟動時偵測系統語言


def test_app_name_follows_language(tmp_path):
    from src import i18n
    from src.config import app_name

    before = i18n.current_language()
    try:
        i18n.set_language("en")
        assert app_name() == "Wizard101 Chat Translator"
        i18n.set_language("zh-TW")
        assert app_name() == "Wizard101 對話翻譯助手"
    finally:
        i18n.set_language(before)


def test_old_config_without_ui_language_loads(tmp_path):
    import json

    from src.config import load_config

    path = tmp_path / "config.json"
    path.write_text(json.dumps({"hotkey": "ctrl+alt+t"}), encoding="utf-8")
    cfg = load_config(path)
    assert cfg["ui_language"] is None       # 舊 config 補上預設值
    assert cfg["hotkey"] == "ctrl+alt+t"    # 既有設定不動
```

- [ ] **Step 2: 執行測試確認失敗**

Run: `uv run pytest tests/test_config.py -v`
Expected: FAIL，`KeyError: 'ui_language'` 與 `ImportError: cannot import name 'app_name'`

- [ ] **Step 3: 修改 `src/config.py`**

把第 8 行的常數換成函式（放在 import 之後）：

```python
from src.i18n import t


def app_name() -> str:
    """應用程式顯示名稱：各視窗標題／工作列統一使用（隨介面語言變動）。"""
    return t("app.name")
```

在 `DEFAULT_CONFIG` 內、`target_language` 之前加入：

```python
    "ui_language": None,     # 介面語言；None＝尚未選過，啟動時依系統語言自動判定
```

- [ ] **Step 4: 更新四個呼叫端的 import 與用法**

`src/reader/overlay.py`：把 `from src.config import APP_NAME` 改為 `from src.config import app_name`，並改三處用法：

```python
        label = tk.Label(bar, text=f"≡  {app_name()}", bg=BAR, fg=FG_BAR,
```

```python
        self._win.title(app_name())  # 工作列按鈕顯示的名稱
```

```python
        b.title(app_name())
```

`src/ui/settings.py`：`from src.config import ADVANCED_LIMITS, app_name, DEFAULT_CONFIG, clamp_advanced`，並把標題改為（本行在 Task 7 會再改成 `t("settings.title", ...)`，此處先讓程式跑得起來）：

```python
        self._win.title(f"{app_name()} — 設定")
```

`src/ui/wizard.py`：`from src.config import app_name`，並：

```python
        self._win.title(f"{app_name()} — 首次設定")
```

`src/composer/input_box.py`：`from src.config import app_name`，並：

```python
        self._win.title(app_name())
```

- [ ] **Step 5: 在 `main.py` 啟動時套用語言**

`src/main.py` 的 import 區加入：

```python
from src.i18n import detect_system_language, set_language
```

在 `cfg = load_config(CONFIG_PATH)` 之後、`root = tk.Tk()` 之前插入：

```python
    # 介面語言要在建立任何視窗之前決定：文案與字型都由它決定。
    set_language(cfg["ui_language"] or detect_system_language())
```

並在啟動摘要的 print 中加入 `ui_language`（第一個欄位，方便回報問題時一眼看到）：

```python
    print(f"[app] startup; frozen={getattr(sys, 'frozen', False)}, "
          f"ui_language={cfg['ui_language']} (active={current_language()}), "
          f"provider={cfg['api']['provider']}, model={cfg['api']['model']}, "
          f"target_language={cfg['target_language']}, hotkey={cfg['hotkey']}, "
          f"poll_interval={cfg['poll_interval']}, "
          f"parallel={cfg['max_parallel_translations']}", file=sys.stderr)
```

import 改為 `from src.i18n import current_language, detect_system_language, set_language`。

- [ ] **Step 6: 執行測試確認通過**

Run: `uv run pytest`
Expected: PASS（全部）

- [ ] **Step 7: Commit**

```bash
git add src/config.py src/main.py src/reader/overlay.py src/ui/settings.py src/ui/wizard.py src/composer/input_box.py tests/test_config.py
git commit -m "feat(config): add ui_language setting and translatable app name"
```

---

### Task 4: overlay 狀態／橫幅改用 key

overlay 是常駐視窗，狀態指示與錯誤橫幅必須能在語言切換後就地重繪。做法是讓 overlay 記住 **state key**（而不是已翻好的字串），顯示時才翻譯。顏色屬於顯示層，一併搬進 overlay。

**Files:**
- Modify: `src/reader/overlay.py:852-856`（`set_status`）、`:866-871`（`set_error`）、模組層新增顏色表
- Modify: `src/main.py:30-41`（刪常數）、`:83-89`（`set_status`）、`:122-130`（`set_banner`）、`:44-53`（`banner_for`）、`:162`、`:166`、`:254`
- Modify: `src/translation_pool.py:22-25`、`:138`（`failed_notice` 改 callable）
- Modify: `tests/test_overlay.py:34`、`:167-168`、`:190-196`、`:233-238`、`:458`
- Modify: `tests/test_reader_loop.py:43-49`
- Modify: `tests/test_translation_pool.py:56`、`tests/test_parallel_order.py:41`
- Modify: `src/i18n/zh-TW.json`、`zh-CN.json`、`en.json`
- Test: `tests/test_overlay.py`、`tests/test_reader_loop.py`

**Interfaces:**
- Consumes: `src.i18n.t()`
- Produces:
  - `OverlayWindow.set_status(state: str) -> None`（`state` ∈ `locating`／`listening`／`translating`／`waiting_game`）
  - `OverlayWindow.set_error(key: str) -> None`
  - `OverlayWindow.refresh_labels() -> None`
  - `main.banner_for(game_missing: bool, error_state: str | None) -> str | None`（回傳 **key**）
  - `TranslationPool(..., failed_notice_fn: Callable[[], str])`

- [ ] **Step 1: 新增文案（三個語言檔各加 9 條）**

`zh-TW.json`：

```json
  "notice.pending": "翻譯中…",
  "notice.translate_failed": "⚠  這則訊息翻譯不出來",
  "notice.game_missing": "⚠  遊戲未就緒／連線中斷，等待中…",
  "notice.offline": "⚠  翻譯伺服器離線，重試中…",
  "notice.config_error": "⚠  API 設定有誤，請開啟設定（⚙）檢查",
  "status.locating": "●  連線遊戲中…",
  "status.listening": "●  監聽中",
  "status.translating": "●  翻譯中…",
  "status.waiting_game": "●  等待遊戲中…"
```

`zh-CN.json`：

```json
  "notice.pending": "翻译中…",
  "notice.translate_failed": "⚠  这条消息翻译不出来",
  "notice.game_missing": "⚠  游戏未就绪／连接中断，等待中…",
  "notice.offline": "⚠  翻译服务器离线，重试中…",
  "notice.config_error": "⚠  API 设置有误，请打开设置（⚙）检查",
  "status.locating": "●  连接游戏中…",
  "status.listening": "●  监听中",
  "status.translating": "●  翻译中…",
  "status.waiting_game": "●  等待游戏中…"
```

`en.json`：

```json
  "notice.pending": "Translating…",
  "notice.translate_failed": "⚠  Could not translate this message",
  "notice.game_missing": "⚠  Game not ready or disconnected — waiting…",
  "notice.offline": "⚠  Translation server is offline — retrying…",
  "notice.config_error": "⚠  API settings are invalid — open settings (⚙) to check",
  "status.locating": "●  Connecting to game…",
  "status.listening": "●  Listening",
  "status.translating": "●  Translating…",
  "status.waiting_game": "●  Waiting for game…"
```

- [ ] **Step 2: 寫失敗測試**

修改 `tests/test_overlay.py` 現有的三處呼叫，並新增語言切換測試。把第 34 行改為：

```python
    ov.set_error("notice.offline")
```

把第 166-170 行改為：

```python
    assert ov.error_text() is None
    ov.set_error("notice.offline")
    assert ov.error_text() == t("notice.offline")
    ov.clear_error()
    assert ov.error_text() is None
```

把第 190-196 行改為：

```python
def test_set_status_updates_bar_label(root):
    ov = OverlayWindow(root)
    assert ov.status_text() == ""
    ov.set_status("listening")
    assert ov.status_text() == t("status.listening")
    ov.set_status("translating")
    assert ov.status_text() == t("status.translating")
```

把第 233-238 行的兩處 `ov.set_status(...)` 改為 `ov.set_status("locating")`／`ov.set_status("listening")`，斷言改為 `t("status.locating")`。第 458 行改為 `ov.set_error("notice.offline")`。

檔案頂端加入 `from src.i18n import t`。

新增測試：

```python
def test_refresh_labels_retranslates_status_and_banner(root):
    from src import i18n

    before = i18n.current_language()
    try:
        i18n.set_language("zh-TW")
        ov = OverlayWindow(root)
        ov.set_status("listening")
        ov.set_error("notice.offline")
        i18n.set_language("en")
        ov.refresh_labels()
        assert ov.status_text() == "●  Listening"
        assert ov.error_text() == "⚠  Translation server is offline — retrying…"
    finally:
        i18n.set_language(before)
```

- [ ] **Step 3: 執行測試確認失敗**

Run: `uv run pytest tests/test_overlay.py -v`
Expected: FAIL，`AttributeError: 'OverlayWindow' object has no attribute 'refresh_labels'`，以及狀態文字比對不符

- [ ] **Step 4: 修改 `src/reader/overlay.py`**

模組層加入顏色表（放在 `FG_BAR` 之後）：

```python
# 狀態指示的顏色（文字由 i18n 依 state key 取得）
STATUS_COLORS = {
    "locating": "#e0b050",
    "listening": "#7dc87d",
    "translating": "#6fa8dc",
    "waiting_game": "#9a9aa8",
}
```

`__init__` 內在建立 `self._status_label` 之後、`self._error_label` 初始化附近加入兩個狀態欄位：

```python
        self._status_state: str | None = None   # 目前狀態的 key，語言切換後重繪用
        self._error_key: str | None = None      # 目前橫幅的 key，同上
```

同時把標題列 label 存成屬性，`refresh_labels()` 才改得到（第 345-347 行）：

```python
        self._title_label = tk.Label(bar, text=f"≡  {app_name()}", bg=BAR, fg=FG_BAR,
                                     font=ui_font(8), anchor="w")
        self._title_label.pack(side="left", padx=6)
```

第 367 行的 `for w in (bar, label, self._status_label):` 改為 `for w in (bar, self._title_label, self._status_label):`。

改寫 `set_status`／`set_error`，並新增 `refresh_labels`：

```python
    def set_status(self, state: str) -> None:
        """更新狀態指示：標題列右側小字；視窗還沒有任何訊息時，同步大字置中顯示。
        存的是 state key 而非文字——語言切換後 refresh_labels() 才能重新翻譯。"""
        self._status_state = state
        text, color = t(f"status.{state}"), STATUS_COLORS[state]
        self._status_label.configure(text=text, fg=color)
        self._placeholder.configure(text=text, fg=color)
        self._refresh_placeholder()

    def set_error(self, key: str) -> None:
        """顯示錯誤橫幅（傳入文案 key，顯示時才翻譯）。"""
        self.clear_error()
        self._error_key = key
        self._error_label = tk.Label(self._frame, text=t(key), bg=BG, fg=FG_ERROR,
                                     font=ui_font(10, "bold"), anchor="w",
                                     wraplength=self._wrap)
        self._error_label.pack(side="bottom", fill="x", pady=2)

    def clear_error(self) -> None:
        self._error_key = None
        if self._error_label is not None:
            self._error_label.destroy()
            self._error_label = None

    def refresh_labels(self) -> None:
        """介面語言變更後重繪常駐文字（標題列、狀態、錯誤橫幅）與字型。
        已經印在畫面上的訊息不回溯改寫——那是聊天內容，不是介面文字。"""
        self._title_label.configure(text=f"≡  {app_name()}", font=ui_font(8))
        self._status_label.configure(font=ui_font(8))
        self._placeholder.configure(font=ui_font(11))
        self._win.title(app_name())
        if self._bubble is not None:
            self._bubble.title(app_name())
        if self._status_state is not None:
            self.set_status(self._status_state)
        if self._error_key is not None:
            self.set_error(self._error_key)
```

檔案頂端加入 `from src.i18n import t` 與 `from src.ui.fonts import ui_font`。

- [ ] **Step 5: 修改 `src/main.py`**

刪除第 30-41 行的五個 NOTICE 常數與 `STATUS` dict。`banner_for` 改為回傳 key：

```python
def banner_for(game_missing: bool, error_state: str | None) -> str | None:
    """依目前狀況決定該顯示哪一條錯誤橫幅的文案 key（None＝不顯示）。
    遊戲未就緒優先於翻譯錯誤：連不上遊戲時翻譯狀態已無意義。"""
    if game_missing:
        return "notice.game_missing"
    if error_state == "config":
        return "notice.config_error"
    if error_state == "offline":
        return "notice.offline"
    return None
```

`reader_loop` 內的 `set_status` 改為直接把 state 交給 overlay：

```python
    def set_status(key: str) -> None:
        nonlocal last_status
        if key == last_status:
            return
        last_status = key
        ui_queue.put(lambda k=key: overlay.set_status(k))
```

`set_banner` 的參數改名為 key（行為不變，傳的是 key）：

```python
    def set_banner(key: str | None) -> None:
        nonlocal last_banner
        if key == last_banner:
            return
        last_banner = key
        if key is None:
            ui_queue.put(overlay.clear_error)
        else:
            ui_queue.put(lambda k=key: overlay.set_error(k))
```

第 162 行的佔位文字改為呼叫當下取字串：

```python
            ui_queue.put(lambda o=line.text, c=line.color, m=msg_id:
                         overlay.add_message(o, t("notice.pending"), msg_id=m,
                                             pending=True, color=c))
```

第 254 行的 pool 建構改為傳 callable：

```python
        failed_notice_fn=lambda: t("notice.translate_failed"))
```

import 區加入 `t`：`from src.i18n import current_language, detect_system_language, set_language, t`。

- [ ] **Step 6: 修改 `src/translation_pool.py`**

第 22-25 行：

```python
    def __init__(self, translator, on_result, workers: int, failed_notice_fn):
        self._translator = translator
        self._on_result = on_result
        # 取失敗提示的 callable 而非字串：介面語言可能在執行中被改掉，
        # 建構當下就定案的字串會停在舊語言。
        self._failed_notice_fn = failed_notice_fn
```

第 138 行：

```python
                    self._on_result(msg_id, self._failed_notice_fn(), True)
```

類別 docstring 的 `failed＝放棄該則、text 是失敗提示而非譯文。` 維持不變。

- [ ] **Step 7: 修改受影響的測試**

`tests/test_translation_pool.py:56` 與 `tests/test_parallel_order.py:41`：把 `failed_notice=FAILED` 改為 `failed_notice_fn=lambda: FAILED`。

`tests/test_reader_loop.py:43-50` 的 FakeOverlay 兩個方法改成收 key（`clear_error` 維持 `self.clears += 1` 不動）：

```python
    def set_error(self, key):
        self.errors.append(key)

    def set_status(self, state):
        self.statuses.append(state)
```

同檔六處斷言改成比對 key（第 138、155、186、198、252、254 行）：

```python
    assert ov.errors == ["notice.offline"]
```

```python
    assert "status.translating" in ov.statuses
```

```python
    assert ov.statuses == ["status.listening", "status.translating", "status.listening"]
```

```python
    assert ov.statuses == ["status.locating"]  # 狀態未變不重複發
```

```python
    assert ov.errors == ["notice.game_missing"]
```

```python
    assert "status.waiting_game" in ov.statuses
```

第 138 與 252 行原本引用的 `main_module.OFFLINE_NOTICE`／`main_module.GAME_MISSING_NOTICE` 已在 Step 5 刪除，必須一併改掉，否則 `AttributeError`。

- [ ] **Step 8: 執行測試確認通過**

Run: `uv run pytest`
Expected: PASS（全部）

- [ ] **Step 9: Commit**

```bash
git add src/reader/overlay.py src/main.py src/translation_pool.py src/i18n tests/
git commit -m "refactor(overlay): key-based status and banner text for live language switching"
```

---

### Task 5: fields 純邏輯函式改回傳 key

表單驗證與錯誤分類是純邏輯，不該碰 i18n。改成回傳 key，由顯示端翻譯——測試也跟著變成語言無關。

`friendly_error` 的兩種錯誤帶變數（HTTP 狀態碼、例外訊息），單一 key 表達不了，改回傳 `(key, kwargs)`。

**Files:**
- Modify: `src/ui/fields.py:48-63`（`validate_*`）、`:90-100`（`friendly_error`）、`:256-279`（呼叫端）、`:431`、`:446`
- Modify: `src/i18n/zh-TW.json`、`zh-CN.json`、`en.json`
- Test: `tests/test_fields.py:35-58`、`:110-137`、`:195-201`

**Interfaces:**
- Consumes: `src.i18n.t()`
- Produces:
  - `validate_endpoint_fields(api: dict) -> list[str]`（key 列表）
  - `validate_api_form(api: dict) -> list[str]`（key 列表）
  - `friendly_error(exc: Exception) -> tuple[str, dict]`（key 與 format 變數）

- [ ] **Step 1: 新增文案（三個語言檔各加 9 條）**

`zh-TW.json`：

```json
  "error.need_api_key": "請輸入 API 金鑰",
  "error.need_base_url": "請輸入伺服器網址",
  "error.need_model": "請選擇或輸入模型",
  "error.bad_key": "金鑰無效或過期，請確認 API 金鑰",
  "error.model_not_found": "找不到模型，請確認模型名稱",
  "error.api_http": "API 設定有誤（HTTP {status}），請檢查各欄位",
  "error.offline": "無法連線到伺服器，請檢查網址與網路",
  "error.unexpected": "發生錯誤：{error}",
  "sep.errors": "；"
```

`zh-CN.json`：

```json
  "error.need_api_key": "请输入 API 密钥",
  "error.need_base_url": "请输入服务器网址",
  "error.need_model": "请选择或输入模型",
  "error.bad_key": "密钥无效或已过期，请确认 API 密钥",
  "error.model_not_found": "找不到模型，请确认模型名称",
  "error.api_http": "API 设置有误（HTTP {status}），请检查各字段",
  "error.offline": "无法连接到服务器，请检查网址与网络",
  "error.unexpected": "发生错误：{error}",
  "sep.errors": "；"
```

`en.json`：

```json
  "error.need_api_key": "Enter your API key.",
  "error.need_base_url": "Enter the server URL.",
  "error.need_model": "Select or enter a model.",
  "error.bad_key": "The API key is invalid or expired. Check your API key.",
  "error.model_not_found": "Model not found. Check the model name.",
  "error.api_http": "API settings are invalid (HTTP {status}). Check each field.",
  "error.offline": "Cannot reach the server. Check the URL and your network.",
  "error.unexpected": "Something went wrong: {error}",
  "sep.errors": "; "
```

- [ ] **Step 2: 改寫測試**

`tests/test_fields.py` 第 35-58 行改為：

```python
def test_validate_requires_model():
    errs = validate_api_form({"provider": "openai", "model": "", "api_key": "k",
                              "base_url": "", "thinking": False})
    assert "error.need_model" in errs


def test_validate_requires_key_for_official_providers():
    errs = validate_api_form({"provider": "claude", "model": "claude-opus-5",
                              "api_key": "", "base_url": "", "thinking": False})
    assert "error.need_api_key" in errs


def test_validate_requires_base_url_for_custom_only():
    api = {"provider": "custom", "model": "m", "api_key": "", "base_url": "",
           "thinking": False}
    assert "error.need_base_url" in validate_api_form(api)
    api["base_url"] = "http://127.0.0.1:8000"
    assert validate_api_form(api) == []  # custom 不需金鑰


def test_friendly_error_messages():
    assert friendly_error(TranslatorConfigError("HTTP 401", status=401)) == \
        ("error.bad_key", {})
    assert friendly_error(TranslatorConfigError("HTTP 404", status=404)) == \
        ("error.model_not_found", {})
    assert friendly_error(TranslatorConfigError("HTTP 500", status=500)) == \
        ("error.api_http", {"status": 500})
    assert friendly_error(TranslatorOffline("refused")) == ("error.offline", {})
```

第 117 行（`test_validate_endpoint_fields_ignores_model`）的斷言改為：

```python
    assert "error.need_api_key" in errs
```

另外三處狀態文字斷言改為比對整句（第 124 行的 `assert "2" in ...` 是數量，語言無關，保留不動）：

第 130 行（`test_model_field_unsupported_endpoint_hints_manual_input`）：

```python
    assert fields._model_field.status() == t("hint.model_no_list")
```

第 137 行（`test_model_field_error_uses_friendly_message`）：

```python
    assert fields._model_field.status() == t("error.offline")
```

第 200 行（`test_model_field_status_says_custom_name_is_allowed`）：

```python
    assert fields._model_field.status() == t("hint.model_found", count=2)
```

檔案頂端加入 `from src.i18n import t`。

- [ ] **Step 3: 執行測試確認失敗**

Run: `uv run pytest tests/test_fields.py -v`
Expected: FAIL，`assert '請輸入 API 金鑰' in [...]` 之類的比對錯誤

- [ ] **Step 4: 修改 `src/ui/fields.py` 的純邏輯函式**

```python
def validate_endpoint_fields(api: dict) -> list[str]:
    """檢查連上端點所需的欄位（不含模型），回傳錯誤文案 key 列表（空＝通過）。
    取模型清單時模型欄本來就還沒填，故與 validate_api_form 分開。"""
    errors = []
    provider = PROVIDERS[api["provider"]]
    if not provider.needs_base_url and not api["api_key"].strip():
        errors.append("error.need_api_key")
    if provider.needs_base_url and not api["base_url"].strip():
        errors.append("error.need_base_url")
    return errors


def validate_api_form(api: dict) -> list[str]:
    """檢查 API 表單必填欄位，回傳錯誤文案 key 列表（空＝通過）。"""
    errors = [] if api["model"].strip() else ["error.need_model"]
    return errors + validate_endpoint_fields(api)


def friendly_error(exc: Exception) -> tuple[str, dict]:
    """把翻譯例外轉成（文案 key， format 變數）。

    帶變數的兩種錯誤（HTTP 狀態碼、未預期例外）光靠 key 表達不了，故回傳
    tuple；顯示端一律 `t(key, **kwargs)`。"""
    if isinstance(exc, TranslatorConfigError):
        if exc.status in (401, 403):
            return "error.bad_key", {}
        if exc.status == 404:
            return "error.model_not_found", {}
        return "error.api_http", {"status": exc.status}
    if isinstance(exc, TranslatorOffline):
        return "error.offline", {}
    return "error.unexpected", {"error": exc}
```

- [ ] **Step 5: 更新 `src/ui/fields.py` 的三個呼叫端**

`ModelField.show_error`（第 260-266 行）：

```python
    def show_error(self, exc: Exception) -> None:
        self._all_models = []
        self._combo.configure(values=[])
        if isinstance(exc, TranslatorNoModelList):
            self._set_status(t("hint.model_no_list"))
        else:
            key, kwargs = friendly_error(exc)
            self._set_status(t(key, **kwargs), error=True)
```

`ModelField._start_refresh`（第 276 行）：

```python
            self._set_status(t("sep.errors").join(t(e) for e in errors), error=True)
```

`ApiFields._start_test`（第 431 行）：

```python
            self._show_test_result(False, t("sep.errors").join(t(e) for e in errors))
```

`ApiFields._test_worker`（第 446 行）：

```python
            key, kwargs = friendly_error(exc)
            self._queue.put((False, t(key, **kwargs)))
```

檔案頂端加入 `from src.i18n import t`。

- [ ] **Step 6: 執行測試確認通過**

Run: `uv run pytest tests/test_fields.py -v`
Expected: PASS

- [ ] **Step 7: 執行全部測試**

Run: `uv run pytest`
Expected: PASS（全部）

> `src/ui/settings.py:168-174` 目前把 `validate_api_form` 的回傳直接丟進 messagebox，此時會顯示 key 字串。Task 7 會修好；本任務只要 pytest 全綠即可。

- [ ] **Step 8: Commit**

```bash
git add src/ui/fields.py src/i18n tests/test_fields.py
git commit -m "refactor(fields): return message keys from validation and error mapping"
```

---

### Task 6: fields UI 文案與字型

把 `fields.py` 剩下的所有顯示文字搬進語言檔，新增介面語言下拉，並補上自訂端點的說明。

**Files:**
- Modify: `src/ui/fields.py:18-45`（`Provider`、常數）、`:119-124`、`:253-301`、`:327-343`、`:377-420`、`:450-454`、`:479-518`
- Modify: `src/i18n/zh-TW.json`、`zh-CN.json`、`en.json`
- Test: `tests/test_fields.py`

**Interfaces:**
- Consumes: `src.i18n.t()`、`src.i18n.LANGUAGES`、`src.ui.fonts.ui_font()`
- Produces:
  - `Provider.label_key: str`（取代 `label`）
  - `UiLanguageField(parent, initial: str)` 具 `value() -> str`（回傳語言碼）與 `set_value(code: str)`
  - `COMMON_LANGUAGES` 追加 `"English"`

- [ ] **Step 1: 新增文案（三個語言檔各加 18 條）**

`zh-TW.json`：

```json
  "provider.custom": "自訂端點（OpenAI API 相容）",
  "field.model": "模型",
  "field.base_url": "伺服器網址",
  "field.api_key": "API 金鑰",
  "field.api_key_optional": "API 金鑰（選填）",
  "field.thinking": "啟用模型思考（thinking）",
  "field.auto_input": "遊戲開啟聊天輸入框時自動呼出翻譯輸入（關閉時自動收回）",
  "field.ui_language": "介面語言",
  "button.refresh": "重新整理",
  "button.loading": "讀取中…",
  "button.test": "測試連線",
  "button.testing": "測試中…",
  "button.show": "顯示",
  "button.change": "更改",
  "button.press_key": "請按鍵…（Esc 取消）",
  "link.get_key": "取得金鑰 ↗",
  "test.success": "連線成功　範例：{sample}",
  "hint.model_idle": "按「重新整理」從伺服器取得可用模型清單；也可以自行輸入模型名稱。",
  "hint.model_no_list": "此端點未提供模型清單，請自行輸入模型名稱。",
  "hint.model_empty": "伺服器回報 0 個可用模型，請自行輸入模型名稱。",
  "hint.model_found": "找到 {count} 個模型：輸入關鍵字可篩選，清單外的模型名稱也能自行輸入。",
  "hint.language": "清單只是常用選項，也可以直接輸入任何語言名稱（例如 Italiano、ภาษาไทย）。",
  "hint.thinking": "建議關閉：開啟可能會讓每則翻譯慢上數秒、也更耗 Token。",
  "hint.custom_endpoint": "填入任何 OpenAI API 相容端點的網址，例如 LM Studio、Ollama、vLLM。"
```

`zh-CN.json`：

```json
  "provider.custom": "自定义端点（OpenAI API 兼容）",
  "field.model": "模型",
  "field.base_url": "服务器网址",
  "field.api_key": "API 密钥",
  "field.api_key_optional": "API 密钥（选填）",
  "field.thinking": "启用模型思考（thinking）",
  "field.auto_input": "游戏打开聊天输入框时自动呼出翻译输入（关闭时自动收回）",
  "field.ui_language": "界面语言",
  "button.refresh": "刷新",
  "button.loading": "读取中…",
  "button.test": "测试连接",
  "button.testing": "测试中…",
  "button.show": "显示",
  "button.change": "更改",
  "button.press_key": "请按键…（Esc 取消）",
  "link.get_key": "获取密钥 ↗",
  "test.success": "连接成功　示例：{sample}",
  "hint.model_idle": "点“刷新”从服务器获取可用模型列表；也可以自行输入模型名称。",
  "hint.model_no_list": "此端点未提供模型列表，请自行输入模型名称。",
  "hint.model_empty": "服务器报告 0 个可用模型，请自行输入模型名称。",
  "hint.model_found": "找到 {count} 个模型：输入关键字可筛选，列表外的模型名称也能自行输入。",
  "hint.language": "列表只是常用选项，也可以直接输入任何语言名称（例如 Italiano、ภาษาไทย）。",
  "hint.thinking": "建议关闭：开启可能会让每条翻译慢上数秒，也更耗 Token。",
  "hint.custom_endpoint": "填入任何 OpenAI API 兼容端点的网址，例如 LM Studio、Ollama、vLLM。"
```

`en.json`：

```json
  "provider.custom": "Custom endpoint (OpenAI-compatible)",
  "field.model": "Model",
  "field.base_url": "Server URL",
  "field.api_key": "API key",
  "field.api_key_optional": "API key (optional)",
  "field.thinking": "Enable model thinking",
  "field.auto_input": "Open the translation input when the game chat box opens (and hide it when it closes)",
  "field.ui_language": "Interface language",
  "button.refresh": "Refresh",
  "button.loading": "Loading…",
  "button.test": "Test connection",
  "button.testing": "Testing…",
  "button.show": "Show",
  "button.change": "Change",
  "button.press_key": "Press a key… (Esc to cancel)",
  "link.get_key": "Get a key ↗",
  "test.success": "Connected. Sample: {sample}",
  "hint.model_idle": "Click Refresh to fetch the model list from the server, or type a model name yourself.",
  "hint.model_no_list": "This endpoint does not provide a model list. Enter the model name yourself.",
  "hint.model_empty": "The server reported 0 available models. Enter the model name yourself.",
  "hint.model_found": "Found {count} models. Type to filter, or enter a name that is not listed.",
  "hint.language": "The list is just common choices — you can type any language name, for example Italiano or ภาษาไทย.",
  "hint.thinking": "Best left off: it can add several seconds per translation and uses more tokens.",
  "hint.custom_endpoint": "Enter the URL of any OpenAI-compatible endpoint, such as LM Studio, Ollama, or vLLM."
```

- [ ] **Step 2: 寫失敗測試**

在 `tests/test_fields.py` 加入：

```python
def test_provider_labels_are_translated():
    from src import i18n
    from src.ui.fields import PROVIDERS

    before = i18n.current_language()
    try:
        i18n.set_language("en")
        assert t(PROVIDERS["custom"].label_key) == "Custom endpoint (OpenAI-compatible)"
        i18n.set_language("zh-CN")
        assert t(PROVIDERS["custom"].label_key) == "自定义端点（OpenAI API 兼容）"
    finally:
        i18n.set_language(before)


def test_common_languages_include_english():
    from src.ui.fields import COMMON_LANGUAGES

    assert "English" in COMMON_LANGUAGES


def test_ui_language_field_round_trips_language_code(root):
    from src.ui.fields import UiLanguageField

    field = UiLanguageField(root, "zh-TW")
    assert field.value() == "zh-TW"
    field.set_value("en")
    assert field.value() == "en"
```

- [ ] **Step 3: 執行測試確認失敗**

Run: `uv run pytest tests/test_fields.py -v`
Expected: FAIL，`AttributeError: 'Provider' object has no attribute 'label_key'`

- [ ] **Step 4: 修改 `src/ui/fields.py` 的常數區**

```python
@dataclass(frozen=True)
class Provider:
    label_key: str
    needs_base_url: bool
    key_url: str | None = None


PROVIDERS: dict[str, Provider] = {
    # 前兩家是品牌名，不進語言檔；只有「自訂端點」需要翻譯。
    "openai": Provider(label_key="provider.openai", needs_base_url=False,
                       key_url="https://platform.openai.com/api-keys"),
    "claude": Provider(label_key="provider.claude", needs_base_url=False,
                       key_url="https://console.anthropic.com/settings/keys"),
    "custom": Provider(label_key="provider.custom", needs_base_url=True),
}

# 翻譯目標語言的常用選項：各語言的 endonym，任何介面語言下都不翻譯。
COMMON_LANGUAGES = ["繁體中文（台灣）", "简体中文（中国）", "English", "日本語",
                    "한국어", "Español", "Português", "Deutsch", "Français"]
```

品牌名仍需 key（三語內容相同），三個語言檔各加：

```json
  "provider.openai": "ChatGPT（OpenAI）",
  "provider.claude": "Claude（Anthropic）"
```

英文版兩條寫成 `"ChatGPT (OpenAI)"` 與 `"Claude (Anthropic)"`（半形括號）。

刪除 `MODEL_HINT_IDLE`／`MODEL_HINT_NO_LIST`／`MODEL_HINT_EMPTY`／`LANGUAGE_HINT`／`THINKING_HINT`／`AUTO_INPUT_LABEL` 六個常數。

- [ ] **Step 5: 把 `fields.py` 內所有顯示文字換成 `t()`、字型換成 `ui_font()`**

`ModelField.__init__`（第 119-124 行）：

```python
        ttk.Label(self, text=t("field.model"), width=14).grid(row=0, column=0, sticky="w")
        self._combo = ttk.Combobox(self, textvariable=self._var, values=[])
        self._combo.grid(row=0, column=1, sticky="ew", pady=2)
        self._btn = ttk.Button(self, text=t("button.refresh"), width=9,
                               command=self._start_refresh)
        self._btn.grid(row=0, column=2, padx=(4, 0))
        self._status = ttk.Label(self, text=t("hint.model_idle"), foreground="#888888",
                                 justify="left")
```

`ModelField.show_models`（第 253-258 行）：

```python
    def show_models(self, models: list[str]) -> None:
        self._all_models = list(models)
        self._combo.configure(values=self._all_models)
        self._set_status(t("hint.model_found", count=len(models)) if models
                         else t("hint.model_empty"))
```

`_start_refresh` 第 278-279 行：

```python
        self._btn.configure(state="disabled", text=t("button.loading"))
        self._set_status(t("hint.model_idle"))
```

`_on_refreshed` 第 301 行：

```python
        self._btn.configure(state="normal", text=t("button.refresh"))
```

`ApiFields.__init__` 第 327-343 行：

```python
        for key, prov in PROVIDERS.items():
            ttk.Radiobutton(radio_row, text=t(prov.label_key), value=key,
                            variable=self._provider,
                            command=self._rebuild_fields).pack(anchor="w")
```

```python
        self._test_btn = ttk.Button(test_row, text=t("button.test"),
                                    command=self._start_test)
```

第 343 行的預設目標語言改為跟著介面語言走（與 Task 8 的連動規則一致）：

```python
        self._target_language_fn = lambda: DEFAULT_TARGET_LANGUAGE[current_language()]
```

模組層新增這張表（放在 `COMMON_LANGUAGES` 之後）：

```python
# 介面語言 → 翻譯目標語言的預設值：首次設定時讓兩者一致，之後互不干涉。
DEFAULT_TARGET_LANGUAGE = {
    "zh-TW": "繁體中文（台灣）",
    "zh-CN": "简体中文（中国）",
    "en": "English",
}
```

`_rebuild_fields` 第 377-391 行：

```python
        if prov.needs_base_url:
            self._labeled_entry(t("field.base_url"), self._base_url)
            hint = ttk.Label(self._fields, text=t("hint.custom_endpoint"),
                             foreground="#888888", justify="left")
            hint.pack(fill="x", padx=(20, 0))
            bind_wrap(hint)
            self._model_row()
            self._labeled_entry(t("field.api_key_optional"), self._api_key, secret=True)
            self._thinking_row()
        else:
            self._labeled_entry(t("field.api_key"), self._api_key, secret=True)
            self._model_row()
            if self._provider.get() == "openai":
                self._thinking_row()
            link = ttk.Label(self._fields, text=t("link.get_key"), foreground="#4a7ddc",
                             cursor="hand2")
```

`_thinking_row` 第 404-406 行：

```python
        ttk.Checkbutton(self._fields, text=t("field.thinking"),
                        variable=self._thinking).pack(anchor="w", pady=(2, 0))
        hint = ttk.Label(self._fields, text=t("hint.thinking"), foreground="#888888",
                         justify="left")
```

`_labeled_entry` 第 418 行：

```python
            btn = ttk.Button(row, text=t("button.show"), width=5,
```

`_start_test` 第 433 行、`_test_worker` 第 450 行、`_on_tested` 第 454 行：

```python
        self._test_btn.configure(state="disabled", text=t("button.testing"))
```

```python
        self._queue.put((True, t("test.success", sample=sample)))
```

```python
        self._test_btn.configure(state="normal", text=t("button.test"))
```

`HotkeyField` 第 479 行、第 493 行、第 507 行：

```python
        self._btn = ttk.Button(self, text=t("button.change"), width=14,
                               command=self._capture)
```

```python
        self._btn.configure(text=t("button.press_key"), state="disabled")
```

```python
        self._btn.configure(text=t("button.change"), state="normal")
```

`LanguageField` 第 520 行：

```python
        hint = ttk.Label(self, text=t("hint.language"), foreground="#888888",
                         justify="left")
```

- [ ] **Step 6: 新增 `UiLanguageField`**

在 `LanguageField` 之後加入：

```python
class UiLanguageField(ttk.Frame):
    """介面語言：固定三個選項的唯讀下拉。

    顯示 endonym（各語言自稱），對外進出的是語言碼——與 LanguageField
    （翻譯目標語言，可自由輸入任何語言名稱）是不同用途的兩個欄位。"""

    def __init__(self, parent, initial: str, on_change=None):
        super().__init__(parent)
        self._on_change = on_change
        self._names = list(LANGUAGES.values())
        self._codes = list(LANGUAGES)
        self._var = tk.StringVar(value=LANGUAGES.get(initial, LANGUAGES[SOURCE_LANGUAGE]))
        combo = ttk.Combobox(self, textvariable=self._var, values=self._names,
                             state="readonly")
        combo.pack(fill="x")
        combo.bind("<<ComboboxSelected>>", lambda e: self._notify())

    def value(self) -> str:
        """目前選到的語言碼。"""
        name = self._var.get()
        return self._codes[self._names.index(name)] if name in self._names \
            else SOURCE_LANGUAGE

    def set_value(self, code: str) -> None:
        self._var.set(LANGUAGES.get(code, LANGUAGES[SOURCE_LANGUAGE]))

    def _notify(self) -> None:
        if self._on_change is not None:
            self._on_change(self.value())
```

檔案頂端 import 改為：

```python
from src.i18n import LANGUAGES, SOURCE_LANGUAGE, current_language, t
from src.ui.fonts import ui_font
from src.ui.responsive import bind_wrap
```

- [ ] **Step 7: 執行測試確認通過**

Run: `uv run pytest tests/test_fields.py -v`
Expected: PASS

- [ ] **Step 8: 執行全部測試**

Run: `uv run pytest`
Expected: PASS（全部）

- [ ] **Step 9: Commit**

```bash
git add src/ui/fields.py src/i18n tests/test_fields.py
git commit -m "feat(fields): translate all field labels and add interface language picker"
```

---

### Task 7: 設定視窗

把設定視窗全部文案搬進語言檔，並在「基本」分頁最上方加入介面語言下拉；儲存時套用新語言。

**Files:**
- Modify: `src/ui/settings.py`（整檔文案）、`:50`、`:62-108`、`:111-134`、`:142-155`、`:162-188`
- Modify: `src/i18n/zh-TW.json`、`zh-CN.json`、`en.json`
- Test: `tests/test_settings.py`

**Interfaces:**
- Consumes: `src.i18n.t()`、`src.i18n.set_language()`、`src.ui.fields.UiLanguageField`
- Produces: `SettingsWindow._save()` 會寫入 `cfg["ui_language"]` 並在呼叫 `on_save()` 前套用語言

- [ ] **Step 1: 新增文案（三個語言檔各加 22 條）**

`zh-TW.json`：

```json
  "settings.title": "{app} — 設定",
  "settings.tab.basic": "基本",
  "settings.tab.advanced": "進階",
  "settings.target_language": "翻譯目標語言",
  "settings.hotkey": "呼出輸入框的熱鍵",
  "settings.poll_interval": "輪詢間隔（秒）",
  "settings.poll_interval_hint": "收訊掃描頻率，小＝更即時",
  "settings.fade": "訊息淡出（秒）",
  "settings.fade_hint": "0＝永不淡出，可滾動看歷史",
  "settings.max_messages": "訊息保留上限",
  "settings.max_messages_hint": "超過移除最舊",
  "settings.parallel": "同時翻譯則數",
  "settings.parallel_hint": "1＝逐則排隊；大於 1 同時翻多則，訊息密集時更快跟上",
  "settings.type_delay": "鍵入延遲（秒）",
  "settings.type_delay_hint": "遊戲漏字就調大",
  "settings.alpha": "視窗不透明度",
  "settings.alpha_hint": "即時預覽，小＝更透明（預設 {default}）",
  "settings.range_hint": "{hint}（範圍 {lo}–{hi}，預設 {default}）",
  "settings.game_path": "遊戲路徑",
  "settings.game_path_hint": "留空＝自動偵測執行中的遊戲",
  "button.browse": "瀏覽…",
  "button.cancel": "取消",
  "button.save": "儲存",
  "error.advanced_not_number": "進階數值格式錯誤，請輸入數字",
  "error.need_target_language": "請選擇或輸入翻譯目標語言",
  "dialog.incomplete_title": "設定不完整",
  "dialog.notice_title": "提示",
  "dialog.game_path_restart": "遊戲路徑將於下次啟動生效"
```

`zh-CN.json`：

```json
  "settings.title": "{app} — 设置",
  "settings.tab.basic": "基本",
  "settings.tab.advanced": "高级",
  "settings.target_language": "翻译目标语言",
  "settings.hotkey": "呼出输入框的热键",
  "settings.poll_interval": "轮询间隔（秒）",
  "settings.poll_interval_hint": "接收扫描频率，小＝更实时",
  "settings.fade": "消息淡出（秒）",
  "settings.fade_hint": "0＝永不淡出，可滚动查看历史",
  "settings.max_messages": "消息保留上限",
  "settings.max_messages_hint": "超过则移除最旧的",
  "settings.parallel": "同时翻译条数",
  "settings.parallel_hint": "1＝逐条排队；大于 1 同时翻译多条，消息密集时更快跟上",
  "settings.type_delay": "键入延迟（秒）",
  "settings.type_delay_hint": "游戏丢字就调大",
  "settings.alpha": "窗口不透明度",
  "settings.alpha_hint": "实时预览，小＝更透明（默认 {default}）",
  "settings.range_hint": "{hint}（范围 {lo}–{hi}，默认 {default}）",
  "settings.game_path": "游戏路径",
  "settings.game_path_hint": "留空＝自动检测运行中的游戏",
  "button.browse": "浏览…",
  "button.cancel": "取消",
  "button.save": "保存",
  "error.advanced_not_number": "高级数值格式错误，请输入数字",
  "error.need_target_language": "请选择或输入翻译目标语言",
  "dialog.incomplete_title": "设置不完整",
  "dialog.notice_title": "提示",
  "dialog.game_path_restart": "游戏路径将在下次启动时生效"
```

`en.json`：

```json
  "settings.title": "{app} — Settings",
  "settings.tab.basic": "Basic",
  "settings.tab.advanced": "Advanced",
  "settings.target_language": "Translate messages into",
  "settings.hotkey": "Hotkey to open the input box",
  "settings.poll_interval": "Poll interval (s)",
  "settings.poll_interval_hint": "How often incoming chat is scanned; lower is more responsive",
  "settings.fade": "Message fade-out (s)",
  "settings.fade_hint": "0 = never fade; scroll back through history",
  "settings.max_messages": "Message limit",
  "settings.max_messages_hint": "Oldest messages are removed beyond this",
  "settings.parallel": "Parallel translations",
  "settings.parallel_hint": "1 = one at a time; higher values translate several at once and keep up better in busy chat",
  "settings.type_delay": "Typing delay (s)",
  "settings.type_delay_hint": "Increase this if the game drops characters",
  "settings.alpha": "Window opacity",
  "settings.alpha_hint": "Live preview; lower is more transparent (default {default})",
  "settings.range_hint": "{hint} (range {lo}–{hi}, default {default})",
  "settings.game_path": "Game path",
  "settings.game_path_hint": "Leave empty to auto-detect the running game",
  "button.browse": "Browse…",
  "button.cancel": "Cancel",
  "button.save": "Save",
  "error.advanced_not_number": "Advanced values must be numbers.",
  "error.need_target_language": "Select or enter a target language.",
  "dialog.incomplete_title": "Incomplete settings",
  "dialog.notice_title": "Notice",
  "dialog.game_path_restart": "The game path takes effect the next time the app starts."
```

- [ ] **Step 2: 寫失敗測試**

在 `tests/test_settings.py` 加入：

```python
def test_parse_advanced_values_returns_error_key():
    import tkinter as tk

    from src.ui.settings import parse_advanced_values

    class BadVar:
        def get(self):
            raise ValueError("not a number")

    values, error = parse_advanced_values(BadVar(), BadVar(), BadVar(), BadVar(),
                                          BadVar(), BadVar())
    assert values is None
    assert error == "error.advanced_not_number"


def test_save_applies_ui_language(root, tmp_path):
    from src import i18n
    from src.config import DEFAULT_CONFIG
    from src.ui.settings import SettingsWindow

    before = i18n.current_language()
    try:
        i18n.set_language("zh-TW")
        cfg = copy.deepcopy(DEFAULT_CONFIG)
        cfg["api"] = {"provider": "custom", "base_url": "http://x", "model": "m",
                      "api_key": "", "thinking": False}
        saved = []
        win = SettingsWindow(root, cfg, on_save=lambda: saved.append(True))
        win.open()
        win._ui_language.set_value("en")
        win._save()
        assert cfg["ui_language"] == "en"
        assert i18n.current_language() == "en"
        assert saved == [True]      # 語言先套用，on_save 才被呼叫
    finally:
        i18n.set_language(before)
```

檔案頂端補上 `import copy`（若尚未 import）。

- [ ] **Step 3: 執行測試確認失敗**

Run: `uv run pytest tests/test_settings.py -v`
Expected: FAIL，`assert '進階數值格式錯誤，請輸入數字' == 'error.advanced_not_number'`

- [ ] **Step 4: 修改 `src/ui/settings.py`**

`parse_advanced_values` 的回傳改成 key（docstring 內的說明一併改為「錯誤文案 key」）：

```python
    except (tk.TclError, ValueError):
        return None, "error.advanced_not_number"
```

`open()` 內第 50 行與「基本」分頁（第 62-76 行）：

```python
        self._win.title(t("settings.title", app=app_name()))
```

```python
        # --- 基本 ---
        basic = ttk.Frame(nb, padding=12)
        nb.add(basic, text=t("settings.tab.basic"))
        ttk.Label(basic, text=t("field.ui_language")).pack(anchor="w")
        self._ui_language = UiLanguageField(basic, current_language())
        self._ui_language.pack(fill="x", pady=(2, 10))
        self._api = ApiFields(basic, cfg["api"])
        self._api.pack(fill="x")
        self._api.set_target_language_fn(lambda: self._language.value())
        ttk.Label(basic, text=t("settings.target_language")).pack(anchor="w", pady=(12, 0))
        self._language = LanguageField(basic, cfg["target_language"])
        self._language.pack(fill="x", pady=(2, 8))
        ttk.Label(basic, text=t("settings.hotkey")).pack(anchor="w")
        self._hotkey = HotkeyField(basic, cfg["hotkey"])
        self._hotkey.pack(anchor="w", pady=(2, 0))
        self._auto_input = tk.BooleanVar(value=cfg["auto_show_input"])
        ttk.Checkbutton(basic, text=t("field.auto_input"),
                        variable=self._auto_input).pack(anchor="w", pady=(10, 0))
```

「進階」分頁（第 79-108 行）：`nb.add(adv, text=t("settings.tab.advanced"))`，六個 `_spin` 的標籤與說明改成傳 key：

```python
        self._poll = self._spin(adv, "settings.poll_interval", cfg["poll_interval"],
                                "poll_interval", 0.1, "settings.poll_interval_hint")
        self._fade = self._spin(adv, "settings.fade", cfg["fade_seconds"],
                                "fade_seconds", 10, "settings.fade_hint")
        self._max_msgs = self._spin(adv, "settings.max_messages", cfg["max_messages"],
                                    "max_messages", 10, "settings.max_messages_hint")
        self._parallel = self._spin(adv, "settings.parallel",
                                    cfg["max_parallel_translations"],
                                    "max_parallel_translations", 1,
                                    "settings.parallel_hint")
        self._type_delay = self._spin(adv, "settings.type_delay", cfg["type_delay"],
                                      "type_delay", 0.01, "settings.type_delay_hint")
```

```python
        ttk.Label(path_row, text=t("settings.game_path"), width=14).pack(side="left")
```

```python
        ttk.Button(path_row, text=t("button.browse"), width=7,
                   command=self._browse_game_path).pack(side="left", padx=(4, 0))
        ttk.Label(adv, text=t("settings.game_path_hint"),
                  foreground="#888888").pack(anchor="w")
```

```python
        ttk.Button(btns, text=t("button.cancel"), command=self._cancel).pack(side="right")
        ttk.Button(btns, text=t("button.save"), command=self._save).pack(side="right",
                                                                        padx=(0, 8))
```

`_spin` 改成收 key（第 142-155 行）：

```python
    def _spin(self, parent, label_key, initial, key, step, hint_key):
        lo, hi = ADVANCED_LIMITS[key]
        row = ttk.Frame(parent)
        row.pack(fill="x", pady=2)
        ttk.Label(row, text=t(label_key), width=14).pack(side="left")
        var = tk.DoubleVar(value=initial) if isinstance(initial, float) \
            else tk.IntVar(value=initial)
        ttk.Spinbox(row, textvariable=var, from_=lo, to=hi, increment=step,
                    width=8).pack(side="left")
        note = ttk.Label(row, text=t("settings.range_hint", hint=t(hint_key), lo=lo,
                                     hi=hi, default=DEFAULT_CONFIG[key]),
                         foreground="#888888", justify="left")
        note.pack(side="left", fill="x", expand=True, padx=8)
        bind_wrap(note)
        return var
```

`_alpha_slider` 第 116 行與第 130 行：

```python
        ttk.Label(row, text=t("settings.alpha"), width=14).pack(side="left")
```

```python
        note = ttk.Label(row, text=t("settings.alpha_hint",
                                     default=DEFAULT_CONFIG["overlay_alpha"]),
                         foreground="#888888", justify="left")
```

`_save`（第 162-188 行）：

```python
    def _save(self) -> None:
        # 進階數值先解析：格式錯誤也要走表單錯誤提示，不能讓 cfg 寫到一半。
        advanced, advanced_error = parse_advanced_values(
            self._poll, self._fade, self._max_msgs, self._type_delay, self._alpha_var,
            self._parallel)
        api = self._api.get_values()
        errors = validate_api_form(api)
        if not self._language.value():
            errors.append("error.need_target_language")
        if advanced_error:
            errors.append(advanced_error)
        if errors:
            messagebox.showwarning(t("dialog.incomplete_title"),
                                   "\n".join(t(e) for e in errors), parent=self._win)
            return
        cfg = self._cfg
        old_game_path = cfg["game_path"]
        cfg["api"] = api
        # 語言要在 on_save 之前套用：apply_settings 會依新語言重繪 overlay。
        cfg["ui_language"] = self._ui_language.value()
        set_language(cfg["ui_language"])
        cfg["target_language"] = self._language.value()
        cfg["hotkey"] = self._hotkey.value()
        cfg["auto_show_input"] = self._auto_input.get()
        cfg.update(advanced)
        cfg["game_path"] = self._game_path.get().strip() or None
        game_path_changed = cfg["game_path"] != old_game_path
        self._on_save()
        if game_path_changed:
            messagebox.showinfo(t("dialog.notice_title"), t("dialog.game_path_restart"),
                                parent=self._win)
        self._win.destroy()
```

import 區改為：

```python
from src.config import ADVANCED_LIMITS, DEFAULT_CONFIG, app_name, clamp_advanced
from src.i18n import current_language, set_language, t
from src.ui.fields import (ApiFields, HotkeyField, LanguageField, UiLanguageField,
                           validate_api_form)
```

- [ ] **Step 5: 執行測試確認通過**

Run: `uv run pytest tests/test_settings.py -v`
Expected: PASS

- [ ] **Step 6: 執行全部測試**

Run: `uv run pytest`
Expected: PASS（全部）

- [ ] **Step 7: Commit**

```bash
git add src/ui/settings.py src/i18n tests/test_settings.py
git commit -m "feat(settings): translate settings window and add interface language control"
```

---

### Task 8: 首次設定精靈的語言步驟

精靈從兩步變三步（語言 → API → 偏好）。語言一改就整個精靈重建，確保沒有任何標籤停在舊語言。

**Files:**
- Modify: `src/ui/wizard.py`（整檔）
- Modify: `src/i18n/zh-TW.json`、`zh-CN.json`、`en.json`
- Test: `tests/test_wizard.py`

**Interfaces:**
- Consumes: `src.i18n.t()`、`src.i18n.set_language()`、`src.i18n.current_language()`、`src.ui.fields.UiLanguageField`、`src.ui.fields.DEFAULT_TARGET_LANGUAGE`
- Produces:
  - `STEP_LANG = 0`、`STEP_API = 1`、`STEP_PREFS = 2`
  - `SetupWizard.restart: bool`
  - `run_wizard(root, cfg) -> bool`（內部重建迴圈，對外行為不變）

- [ ] **Step 1: 新增文案（三個語言檔各加 9 條）**

`zh-TW.json`：

```json
  "wizard.title": "{app} — 首次設定",
  "wizard.step.language": "介面語言",
  "wizard.step.api": "API 設定",
  "wizard.step.prefs": "偏好設定",
  "wizard.language_hint": "選擇本工具介面使用的語言。",
  "wizard.intro": "本工具會即時翻譯 Wizard101 的遊戲聊天，並可用熱鍵輸入你的語言、翻成英文送進遊戲。\n請選擇翻譯服務並填好設定，按「測試連線」確認可用（會實際翻譯一句測試文字）。",
  "wizard.skip_test": "略過測試",
  "wizard.target_language": "翻譯目標語言（收到的訊息翻成什麼語言）",
  "button.back": "上一步",
  "button.next": "下一步",
  "button.finish": "完成"
```

`zh-CN.json`：

```json
  "wizard.title": "{app} — 首次设置",
  "wizard.step.language": "界面语言",
  "wizard.step.api": "API 设置",
  "wizard.step.prefs": "偏好设置",
  "wizard.language_hint": "选择本工具界面使用的语言。",
  "wizard.intro": "本工具会实时翻译 Wizard101 的游戏聊天，并可用热键输入你的语言、翻译成英文发送到游戏中。\n请选择翻译服务并填好设置，点“测试连接”确认可用（会实际翻译一句测试文本）。",
  "wizard.skip_test": "跳过测试",
  "wizard.target_language": "翻译目标语言（收到的消息翻译成什么语言）",
  "button.back": "上一步",
  "button.next": "下一步",
  "button.finish": "完成"
```

`en.json`：

```json
  "wizard.title": "{app} — Setup",
  "wizard.step.language": "Interface language",
  "wizard.step.api": "API settings",
  "wizard.step.prefs": "Preferences",
  "wizard.language_hint": "Choose the language this app uses.",
  "wizard.intro": "This tool translates Wizard101 chat in real time, and lets you type in your own language and send it to the game in English.\nPick a translation service, fill in the settings, then use Test connection to confirm it works (it really does translate a sample line).",
  "wizard.skip_test": "Skip test",
  "wizard.target_language": "Target language (what incoming messages are translated into)",
  "button.back": "Back",
  "button.next": "Next",
  "button.finish": "Finish"
```

- [ ] **Step 2: 寫失敗測試**

`tests/test_wizard.py` 現有內容保留，加入：

```python
def test_can_advance_allows_language_step():
    from src.ui.wizard import STEP_LANG, can_advance

    # 語言頁沒有必填欄位，永遠可以往下一步
    assert can_advance(STEP_LANG, api_test_passed=False, api_errors=["error.need_model"])


def test_language_change_marks_restart_and_applies_language(root):
    import copy

    from src import i18n
    from src.config import DEFAULT_CONFIG
    from src.ui.wizard import SetupWizard

    before = i18n.current_language()
    try:
        i18n.set_language("zh-TW")
        cfg = copy.deepcopy(DEFAULT_CONFIG)
        wizard = SetupWizard(root, cfg)
        wizard._on_language_change("en")
        assert cfg["ui_language"] == "en"
        assert i18n.current_language() == "en"
        assert wizard.restart is True
    finally:
        i18n.set_language(before)


def test_language_change_updates_untouched_target_language(root):
    import copy

    from src import i18n
    from src.config import DEFAULT_CONFIG
    from src.ui.wizard import SetupWizard

    before = i18n.current_language()
    try:
        i18n.set_language("zh-TW")
        cfg = copy.deepcopy(DEFAULT_CONFIG)
        cfg["target_language"] = "繁體中文（台灣）"   # 使用者還沒動過
        wizard = SetupWizard(root, cfg)
        wizard._on_language_change("en")
        assert cfg["target_language"] == "English"
    finally:
        i18n.set_language(before)


def test_language_change_keeps_customised_target_language(root):
    import copy

    from src import i18n
    from src.config import DEFAULT_CONFIG
    from src.ui.wizard import SetupWizard

    before = i18n.current_language()
    try:
        i18n.set_language("zh-TW")
        cfg = copy.deepcopy(DEFAULT_CONFIG)
        cfg["target_language"] = "日本語"   # 使用者自己選過了
        wizard = SetupWizard(root, cfg)
        wizard._on_language_change("en")
        assert cfg["target_language"] == "日本語"
    finally:
        i18n.set_language(before)
```

- [ ] **Step 3: 執行測試確認失敗**

Run: `uv run pytest tests/test_wizard.py -v`
Expected: FAIL，`ImportError: cannot import name 'STEP_LANG'`

- [ ] **Step 4: 改寫 `src/ui/wizard.py`**

模組頂端：

```python
"""首次設定精靈：介面語言 → API 設定（選服務商 → 填 API → 測試連線）→ 偏好設定，
三步完成寫入 cfg。中途關閉＝取消（不留半套設定），run_wizard 回傳 False。"""
import tkinter as tk
from tkinter import ttk

from src.config import app_name
from src.i18n import current_language, set_language, t
from src.ui.fields import (DEFAULT_TARGET_LANGUAGE, ApiFields, HotkeyField,
                           LanguageField, UiLanguageField, validate_api_form)
from src.ui.fonts import ui_font
from src.ui.responsive import bind_wrap

STEP_LANG, STEP_API, STEP_PREFS = 0, 1, 2
_STEP_KEYS = ["wizard.step.language", "wizard.step.api", "wizard.step.prefs"]


def can_advance(step: int, api_test_passed: bool, api_errors: list[str]) -> bool:
    """該步驟是否允許按「下一步」。
    api_test_passed 已含「使用者明示略過測試」的情形（見 SetupWizard._refresh_nav）。"""
    if step == STEP_API:
        return not api_errors and api_test_passed
    return True
```

`__init__` 改動：`self._step = STEP_LANG`、新增 `self.restart = False`、標題與導覽按鈕改 `t()`、字型改 `ui_font()`，並建立語言欄位元件：

```python
        self._win.title(t("wizard.title", app=app_name()))
```

```python
        self._title = ttk.Label(self._win, font=ui_font(13, "bold"))
```

```python
        self._back_btn = ttk.Button(nav, text=t("button.back"), command=self._back)
        self._back_btn.pack(side="left")
        self._next_btn = ttk.Button(nav, text=t("button.next"), command=self._next)
        self._next_btn.pack(side="right")

        # 跨步驟保留的欄位元件（建一次，切步驟時搬進／搬出 body）
        self._ui_language = UiLanguageField(self._body, current_language(),
                                            on_change=self._on_language_change)
        self._api_fields = ApiFields(self._body, cfg["api"], on_change=self._on_api_change)
        self._language = LanguageField(self._body, cfg["target_language"])
        self._hotkey = HotkeyField(self._body, cfg["hotkey"])
        self._auto_input = tk.BooleanVar(value=cfg["auto_show_input"])
        self._show_step()
```

`_show_step` 改為三步：

```python
    def _show_step(self) -> None:
        # 跨步驟保留的元件只收起來；每步臨時建立的說明文字等直接銷毀，
        # 避免來回導航時在 body 底下累積孤兒 widget。
        persistent = {self._ui_language, self._api_fields, self._language, self._hotkey}
        for w in self._body.winfo_children():
            if w in persistent:
                w.pack_forget()
            else:
                w.destroy()
        self._indicator.configure(text="  ".join(
            "●" if i <= self._step else "○" for i in range(len(_STEP_KEYS))))
        self._title.configure(text=t(_STEP_KEYS[self._step]))

        if self._step == STEP_LANG:
            hint = ttk.Label(self._body, text=t("wizard.language_hint"), justify="left")
            hint.pack(fill="x", pady=(0, 8))
            bind_wrap(hint)
            self._ui_language.pack(fill="x")
            self._next_btn.configure(text=t("button.next"))
        elif self._step == STEP_API:
            intro = ttk.Label(self._body, text=t("wizard.intro"), justify="left")
            intro.pack(fill="x")
            bind_wrap(intro)
            self._api_fields.pack(fill="x", pady=(10, 0))
            skip = ttk.Label(self._body, text=t("wizard.skip_test"), foreground="#888888",
                             cursor="hand2", font=ui_font(8))
            skip.pack(anchor="e", pady=(6, 0))
            skip.bind("<Button-1>", lambda e: self._do_skip_test())
            self._next_btn.configure(text=t("button.next"))
        else:  # STEP_PREFS
            ttk.Label(self._body, text=t("wizard.target_language")).pack(anchor="w")
            self._language.pack(fill="x", pady=(2, 12))
            ttk.Label(self._body, text=t("settings.hotkey")).pack(anchor="w")
            self._hotkey.pack(anchor="w", pady=(2, 0))
            ttk.Checkbutton(self._body, text=t("field.auto_input"),
                            variable=self._auto_input).pack(anchor="w", pady=(14, 0))
            self._next_btn.configure(text=t("button.finish"))
        self._back_btn.configure(
            state="normal" if self._step > STEP_LANG else "disabled")
        self._refresh_nav()
```

新增語言變更處理與欄位收集：

```python
    def _on_language_change(self, code: str) -> None:
        """語言一改就整個精靈重建：跨步驟保留的欄位元件已帶著舊語言的標籤，
        逐一刷新容易漏掉，重建最保險（代價是 API 測試狀態要重測）。"""
        if code == current_language():
            return
        old_default = DEFAULT_TARGET_LANGUAGE.get(current_language())
        self._collect_into_cfg()
        set_language(code)
        self._cfg["ui_language"] = code
        # 使用者還沒動過翻譯目標語言時，讓它跟著介面語言走；動過就不覆蓋。
        if self._cfg["target_language"] == old_default:
            self._cfg["target_language"] = DEFAULT_TARGET_LANGUAGE[code]
        self.restart = True
        print(f"[ui] wizard restarting with language {code}", file=sys.stderr)
        self._win.destroy()

    def _collect_into_cfg(self) -> None:
        """把目前填在欄位裡的值寫回 cfg（重建精靈與完成精靈共用）。"""
        self._cfg["api"] = self._api_fields.get_values()
        if self._language.value():
            self._cfg["target_language"] = self._language.value()
        self._cfg["hotkey"] = self._hotkey.value()
        self._cfg["auto_show_input"] = self._auto_input.get()
```

`_finish` 改為共用 `_collect_into_cfg`：

```python
    def _finish(self) -> None:
        self._collect_into_cfg()
        self._cfg["ui_language"] = current_language()
        self.completed = True
        self._win.destroy()
```

`_next` 的終點判斷改為 `STEP_PREFS`（原本即是，確認不變）。`__init__` 內 `self._step = STEP_LANG`、`self.restart = False`。檔案頂端加入 `import sys`。

`run_wizard` 加入重建迴圈：

```python
def run_wizard(root: tk.Tk, cfg: dict) -> bool:
    """顯示首次設定精靈並等待關閉；完成回 True（結果已寫入 cfg，呼叫端負責存檔）。
    使用者在語言頁換語言時，精靈會以新語言重建（見 SetupWizard._on_language_change）。"""
    while True:
        wizard = SetupWizard(root, cfg)
        root.wait_window(wizard._win)
        if not wizard.restart:
            return wizard.completed
```

- [ ] **Step 5: 執行測試確認通過**

Run: `uv run pytest tests/test_wizard.py -v`
Expected: PASS

- [ ] **Step 6: 執行全部測試**

Run: `uv run pytest`
Expected: PASS（全部）

- [ ] **Step 7: Commit**

```bash
git add src/ui/wizard.py src/i18n tests/test_wizard.py
git commit -m "feat(wizard): add interface language step with rebuild on change"
```

---

### Task 9: 翻譯輸入框

**Files:**
- Modify: `src/composer/input_box.py:56-60`、`:118`、`:128`、`:160-164`
- Modify: `src/i18n/zh-TW.json`、`zh-CN.json`、`en.json`
- Test: `tests/test_input_box.py:53`、`:160-195`

**Interfaces:**
- Consumes: `src.i18n.t()`、`src.ui.fonts.ui_font()`
- Produces: 無新介面（`_show_error` 仍收已翻譯字串）

- [ ] **Step 1: 新增文案（三個語言檔各加 4 條）**

`zh-TW.json`：

```json
  "input.hint": "輸入訊息後按下 Enter 會執行翻譯並自動輸入進遊戲輸入框（不會自動送出）；按 Esc 或未輸入直接按 Enter 可關閉此輸入框",
  "input.translating": "翻譯中…",
  "input.failed": "翻譯失敗：{error}",
  "input.too_long": "譯文 {count} 字，超過遊戲上限 {limit} 字——請刪減或分段後重送"
```

`zh-CN.json`：

```json
  "input.hint": "输入消息后按下 Enter 会执行翻译并自动输入到游戏输入框（不会自动发送）；按 Esc 或未输入直接按 Enter 可关闭此输入框",
  "input.translating": "翻译中…",
  "input.failed": "翻译失败：{error}",
  "input.too_long": "译文 {count} 字，超过游戏上限 {limit} 字——请删减或分段后重发"
```

`en.json`：

```json
  "input.hint": "Type a message and press Enter to translate it and type it into the game chat box (it is not sent automatically). Press Esc, or Enter on an empty box, to close this window.",
  "input.translating": "Translating…",
  "input.failed": "Translation failed: {error}",
  "input.too_long": "The translation is {count} characters, over the game limit of {limit} — trim it or split it up."
```

> `input.translating` 與 `notice.pending` 目前字面相同但語境不同（一個是輸入框狀態、一個是訊息佔位），分成兩個 key，讓譯者能分別調整、日後改動不會互相波及。

- [ ] **Step 2: 改寫測試**

`tests/test_input_box.py` 第 53 行改為語言無關的斷言：

```python
    assert str(GAME_INPUT_MAX_CHARS + 1) in box._status.cget("text")
```

第 165-168 行改用中性字串（`_show_error` 收的是已翻譯文字，不必是中文）：

```python
    box._show_error("boom", current_session)

    # Status should be updated to error message
    assert box._status.cget("text") == "boom"
```

第 193 行改為斷言錯誤內容有被帶出來：

```python
    assert "boom" in box._status.cget("text")
```

新增：

```python
def test_error_message_follows_language(root):
    import httpx

    from src import i18n

    before = i18n.current_language()
    try:
        i18n.set_language("en")
        ui_queue = queue.Queue()

        def failing_translate(text):
            raise httpx.HTTPError("boom")

        box = InputBox(root, failing_translate, ui_queue, lambda e, h: None)
        box._win = tk.Toplevel(root)
        box._entry = tk.Entry(box._win)
        box._status = tk.Label(box._win, text="original")
        box._worker("hello", None, box._session)
        ui_queue.get_nowait()()
        assert box._status.cget("text").startswith("Translation failed:")
    finally:
        i18n.set_language(before)
```

- [ ] **Step 3: 執行測試確認失敗**

Run: `uv run pytest tests/test_input_box.py -v`
Expected: FAIL，`assert 'Translation failed:...'`（目前輸出仍是「翻譯失敗：」）

- [ ] **Step 4: 修改 `src/composer/input_box.py`**

```python
        self._entry = tk.Entry(self._win, bg="#262636", fg=FG, insertbackground=FG,
                               font=ui_font(12))
        self._entry.pack(fill="x", padx=8, pady=(10, 4))
        self._status = tk.Label(self._win, text=t("input.hint"),
                                bg=BG, fg="#9a9aa8", font=ui_font(9),
                                anchor="w", justify="left")
```

```python
        self._status.configure(text=t("input.translating"), fg="#9a9aa8")
```

```python
            msg = t("input.failed", error=exc)
```

```python
        if len(translated) > GAME_INPUT_MAX_CHARS:
            # 超過遊戲輸入上限：不鍵入、不關窗，讓使用者刪減原文後重送
            self._show_error(t("input.too_long", count=len(translated),
                               limit=GAME_INPUT_MAX_CHARS), session)
            return
```

檔案頂端加入 `from src.i18n import t` 與 `from src.ui.fonts import ui_font`。

- [ ] **Step 5: 執行測試確認通過**

Run: `uv run pytest tests/test_input_box.py -v`
Expected: PASS

- [ ] **Step 6: 執行全部測試**

Run: `uv run pytest`
Expected: PASS（全部）

- [ ] **Step 7: Commit**

```bash
git add src/composer/input_box.py src/i18n tests/test_input_box.py
git commit -m "feat(composer): translate input box copy and follow language font"
```

---

### Task 10: overlay 字型與語言變更的即時套用

把 overlay 剩下的 8 處硬編字族換成 `ui_font()`，並讓 `apply_settings()` 在語言變更時重繪。

**Files:**
- Modify: `src/reader/overlay.py:29-30`、`:40-51`、`:351-365`、`:399-400`、`:522-524`
- Modify: `src/main.py:270-282`（`apply_settings`）
- Test: `tests/test_overlay.py`

**Interfaces:**
- Consumes: `src.ui.fonts.ui_font()`、`src.reader.overlay.OverlayWindow.refresh_labels()`
- Produces: `apply_settings()` 在 `cfg["ui_language"]` 變更時呼叫 `overlay.refresh_labels()`

- [ ] **Step 1: 寫失敗測試**

在 `tests/test_overlay.py` 加入：

```python
def test_message_font_follows_language(root):
    from src import i18n

    before = i18n.current_language()
    try:
        i18n.set_language("zh-CN")
        ov = OverlayWindow(root)
        ov.add_message("[A] hi", "嗨", msg_id=1)
        row = ov._messages[0].row
        fonts = {str(row.itemcget(item, "font"))
                 for item in row.find_withtag("txt")}
        assert all("YaHei" in f for f in fonts)
    finally:
        i18n.set_language(before)
```

> `_messages[0].row` 是 `_outlined_line` 建立的 canvas；`find_withtag("txt")` 取得描邊與本色的文字項目。若既有測試已有取用訊息 canvas 的輔助函式，改用它。

- [ ] **Step 2: 執行測試確認失敗**

Run: `uv run pytest tests/test_overlay.py::test_message_font_follows_language -v`
Expected: FAIL（字型仍是 Microsoft JhengHei）

- [ ] **Step 3: 修改 `src/reader/overlay.py`**

刪除第 29-30 行的 `_FONT_ORIGINAL`／`_FONT_TRANSLATED` 常數。它們只在第 787、790 行被用到（建立訊息文字行時），改成呼叫當下取字型：

```python
                       ui_font(9), self._wrap).pack(fill="x")
```

```python
                       ui_font(11), self._wrap).pack(fill="x")
```

標題列三個按鈕（第 351-365 行）與 placeholder（第 399-400 行）、泡泡徽章（第 522-524 行）的 `font=("Microsoft JhengHei", N)` 一律改為 `font=ui_font(N)`，`("Microsoft JhengHei", 9, "bold")` 改為 `ui_font(9, "bold")`。

改完後，`overlay.py` 內不應再出現 `"Microsoft JhengHei"` 字串。

- [ ] **Step 4: 在 `apply_settings()` 接上語言變更**

`src/main.py` 的 `apply_settings`：

```python
    def apply_settings() -> None:
        nonlocal hotkey_handle, ui_language
        save_config(CONFIG_PATH, cfg)
        translator.reconfigure(**cfg["api"], target_language=cfg["target_language"])
        pool.resize(cfg["max_parallel_translations"])
        keyboard.remove_hotkey(hotkey_handle)
        hotkey_handle = keyboard.add_hotkey(cfg["hotkey"],
                                            lambda: ui_queue.put(input_box.show))
        overlay.set_limits(cfg["max_messages"], cfg["fade_seconds"])
        overlay.set_alpha(cfg["overlay_alpha"])
        # 語言已由設定視窗套用（set_language）；這裡負責讓常駐的 overlay 跟上。
        if cfg["ui_language"] != ui_language:
            ui_language = cfg["ui_language"]
            overlay.refresh_labels()
            print(f"[ui] overlay relabelled for language {ui_language}", file=sys.stderr)
        print(f"[settings] applied; provider={cfg['api']['provider']}, "
              f"model={cfg['api']['model']}, hotkey={cfg['hotkey']}, "
              f"ui_language={cfg['ui_language']}, "
              f"parallel={cfg['max_parallel_translations']}", file=sys.stderr)
```

在 `apply_settings` 之前（`input_box` 建立之後）加入追蹤變數：

```python
    ui_language = cfg["ui_language"]   # 用來判斷設定視窗是否改過介面語言
```

- [ ] **Step 5: 執行測試確認通過**

Run: `uv run pytest`
Expected: PASS（全部）

- [ ] **Step 6: 手動驗證語言即時切換**

Run: `uv run run.py`
操作：開啟設定（⚙）→ 介面語言改成 English → 儲存
Expected: overlay 標題列變成 `≡  Wizard101 Chat Translator`、狀態指示變成英文、字型變成 Segoe UI，不需重啟。

- [ ] **Step 7: Commit**

```bash
git add src/reader/overlay.py src/main.py tests/test_overlay.py
git commit -m "feat(overlay): apply language-aware fonts and relabel on language change"
```

---

### Task 11: mem_reader 的 log 訊息英文化

這三條是 `GameNotRunning` 的例外訊息，只流向 stderr／`app.log`（`main.py:140`），不是 UI 文字。依 CLAUDE.md「log 訊息一律英文」修正既有違反。

**Files:**
- Modify: `src/reader/mem_reader.py:560`、`:583`、`:594`
- Test: `tests/test_mem_reader.py`

**Interfaces:**
- Consumes: 無
- Produces: 無（僅訊息字串變更）

- [ ] **Step 1: 檢查測試是否斷言這些訊息**

Run: `uv run pytest tests/test_mem_reader.py -v -k "not_running or game"`
確認 `tests/test_mem_reader.py:797` 附近的 `assert False, "應丟 GameNotRunning"` 只是測試自己的失敗訊息（不需改），且沒有測試比對例外訊息內容。若有，改為比對英文。

- [ ] **Step 2: 修改三條訊息**

```python
            raise GameNotRunning(f"chatlog read failed (likely left the game): {exc}") from exc
```

```python
            raise GameNotRunning(f"game process not found: {self.process_name}")
```

```python
            raise GameNotRunning(f"failed to attach to game: {exc}") from exc
```

- [ ] **Step 3: 執行測試確認通過**

Run: `uv run pytest`
Expected: PASS（全部）

- [ ] **Step 4: Commit**

```bash
git add src/reader/mem_reader.py
git commit -m "fix(reader): write GameNotRunning messages in English for the log"
```

---

### Task 12: 防迴歸靜態檢查

固化成測試，讓未來新增文案忘了走 i18n 時直接紅燈。

**Files:**
- Create: `tests/test_no_hardcoded_ui_text.py`
- Test: 同上

**Interfaces:**
- Consumes: 無（純靜態掃描）
- Produces: 無

- [ ] **Step 1: 寫測試**

建立 `tests/test_no_hardcoded_ui_text.py`：

```python
"""防迴歸：UI 模組內不得再出現中日韓文字的字串字面量。

新增文案時忘了走 i18n，這個測試會直接失敗。掃描範圍刻意不含 translator.py
（LLM 提示詞，非 UI 文字）與 mem_reader.py（log 訊息，另有英文規則）。
"""
import ast
from pathlib import Path

import pytest

SCAN_TARGETS = [
    "src/main.py",
    "src/config.py",
    "src/reader/overlay.py",
    "src/composer/input_box.py",
    "src/ui/fields.py",
    "src/ui/settings.py",
    "src/ui/wizard.py",
    "src/ui/fonts.py",
    "src/ui/responsive.py",
]

# 例外：語言選單與翻譯目標語言清單一律顯示 endonym，任何介面語言下都不翻譯。
ALLOWED = {
    "繁體中文（台灣）", "简体中文（中国）", "日本語", "한국어",
}


def _has_cjk(text: str) -> bool:
    return any("぀" <= c <= "鿿" or "가" <= c <= "힯" for c in text)


def _docstring_nodes(tree: ast.AST) -> set[int]:
    """模組／函式／類別的 docstring 節點 id：docstring 用中文是規範，不該被擋。"""
    ids = set()
    for node in ast.walk(tree):
        if isinstance(node, (ast.Module, ast.FunctionDef, ast.AsyncFunctionDef,
                             ast.ClassDef)):
            if ast.get_docstring(node, clean=False) is not None and node.body \
                    and isinstance(node.body[0], ast.Expr):
                ids.add(id(node.body[0].value))
    return ids


@pytest.mark.parametrize("relative_path", SCAN_TARGETS)
def test_no_hardcoded_cjk_string_literals(relative_path):
    path = Path(__file__).resolve().parent.parent / relative_path
    tree = ast.parse(path.read_text(encoding="utf-8"))
    skip = _docstring_nodes(tree)
    offenders = [
        (node.lineno, node.value)
        for node in ast.walk(tree)
        if isinstance(node, ast.Constant) and isinstance(node.value, str)
        and id(node) not in skip and _has_cjk(node.value)
        and node.value not in ALLOWED
    ]
    assert offenders == [], (
        f"{relative_path} 仍有硬編的介面文字，請改走 src.i18n.t()：{offenders}")
```

- [ ] **Step 2: 執行測試**

Run: `uv run pytest tests/test_no_hardcoded_ui_text.py -v`
Expected: PASS。若有失敗，逐一把該行文案搬進三個語言檔並改用 `t()`（`DEFAULT_TARGET_LANGUAGE` 的值屬於 endonym，需要時加進 `ALLOWED`）。

- [ ] **Step 3: 執行全部測試**

Run: `uv run pytest`
Expected: PASS（全部）

- [ ] **Step 4: 三語人工檢視**

Run: `uv run run.py`
逐一切換三種語言，開啟設定視窗與（刪掉 `config.json` 後的）首次設定精靈，確認：

1. 沒有任何欄位標籤或說明被切字（英文字串較長，必要時放寬 `settings.py` 的 `minsize(640, 560)` 與 `wizard.py` 的 `minsize(540, 540)`）。
2. 字型正確（zh-TW 正黑體／zh-CN 雅黑／en Segoe UI）。
3. 簡中沒有殘留台灣用詞（設定→设置、重新整理→刷新、金鑰→密钥、訊息→消息、連線→连接）。
4. 首次精靈在語言頁改語言後以新語言重建，已填的 API 欄位值保留。

若需要調整視窗尺寸，在此步驟一併改掉。

- [ ] **Step 5: Commit**

```bash
git add tests/test_no_hardcoded_ui_text.py src/ui/settings.py src/ui/wizard.py
git commit -m "test(i18n): guard against hardcoded UI text in translated modules"
```

---

## 完成後的驗收

pytest 全綠之外，開著遊戲（登入進世界內）跑一次 `uv run run.py`，確認：

1. overlay 狀態指示、錯誤橫幅在三種語言下都正確顯示。
2. 設定視窗改語言 → 儲存 → overlay 立刻換語言與字型，不需重啟。
3. 刪掉 `config.json` 後首次啟動，介面語言自動跟隨 Windows 系統語言。
4. 熱鍵輸入框的提示、翻譯中、翻譯失敗、超過字數上限四種狀態文字都跟著語言走。
5. `app.log` 裡的訊息全為英文，且含 `[i18n] language set:` 與 `[i18n] system language detected:` 兩行。
